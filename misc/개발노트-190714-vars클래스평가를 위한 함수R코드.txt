names(vr)
class(vr)

# 자유도/분포/축소추정에 영향받는 함수는 *표시
coef(vr)
fevd(vr)
fitted(vr)
plot(irf(vr))
logLik(vr)		#*
AIC(vr)		#*
BIC(vr)		#*
Phi(vr)
plot(vr)
predict(vr)
print(vr)		#*  (optional) 계수 이외에 추가적인 정보 print 여부
Psi(vr)
resid(vr)
summary(vr)	#* 수정할지 여부: Std. Error     t value     Pr(>|t|)     df.residuals,    R^2,    R^2(adj)    F-stat & pvalue   (정규분포가정)



Acoef(vr)
Bcoef(vr) 
arch.test(vr, lags.single = 1)
BQ(vr)
causality(vr, cause = "U")
normality.test(vr) 
restrict(vr) 	#NOT VALID
roots(vr)
serial.test(vr)
stability(vr)